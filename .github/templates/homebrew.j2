# 📦 Camalot Homebrew Tap

Welcome to Camalot's [Homebrew Tap](https://github.com/Homebrew/homebrew-cask). This repository contains custom manifests for applications not available in the main buckets.

## 🚀 How to Install Camalot's Homebrew Tap

To add this tap to your local Homebrew installation, open your terminal and run:

```shell
brew tap {{ env['ORG'] }}/{{ env['REPO'] }}
```

## 🛠️ Applications

<table>
  <thead>
    <tr>
      <th>Application</th>
      <th>Description</th>
      <th>Version</th>
      <th>License</th>
      <th>Manifest Link</th>
    </tr>
  </thead>
  <tbody>
  {%- for app in apps %}
    <tr>
      <td><strong><a href="{{ app.homepage }}">{{ app.name }}</a></strong></td>
      <td>{{ app.description }}</td>
      <td><code>{{ app.version }}</code></td>
      <td>{{ app.license }}</td>
      <td><a href="{{ app.name }}.json">JSON</a></td>
    </tr>
    <tr>
      <td colspan="5">
        <pre><code>brew install {{ env['ORG'] }}/{{ env['REPO'] }}/{{ app.name }}</code></pre>
      </td>
    </tr>
  {%- endfor %}
  </tbody>
</table>
